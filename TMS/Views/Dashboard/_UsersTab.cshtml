@*@model IEnumerable<TMS.Models.UserViewModel>

<style>
    /* Custom Scrollbar */
    .flex-grow-1.overflow-auto::-webkit-scrollbar {
        width: 5px; /* Make scrollbar thin */
    }

    .flex-grow-1.overflow-auto::-webkit-scrollbar-thumb {
        background-color: rgba(255, 255, 255, 0.5); /* White semi-transparent */
        border-radius: 5px; /* Rounded edges */
    }

    .flex-grow-1.overflow-auto::-webkit-scrollbar-track {
        background: transparent; /* No background */
    }

    /* Ensure you can scroll fully to the bottom */
    .flex-grow-1.overflow-auto {
        padding-bottom: 20px; /* Extra space to see last item */
        scrollbar-gutter: stable; /* Prevents layout shift when scrollbar appears */
    }
</style>


<div class="position-fixed top-0 end-0 d-flex flex-column bg-dark vh-100 p-3 shadow w-10">

    <form class="d-flex mb-3 pt-5" role="search">
        <input class="form-control form-control-sm bg-secondary text-black border-0" name="searchQuery" value="@ViewBag.SearchQuery" type="search" placeholder="Search users..." aria-label="Search">
        <button class="btn btn-sm btn-outline-light ms-2" type="submit">
            <i class="bi bi-search"></i>
        </button>
    </form>

    <div class="d-flex justify-content-between align-items-center mb-2">
        <h6 class="text-white mb-0">Users</h6>
        <small class="text-white">Total: <strong>@ViewBag.TotalUsers</strong></small>
    </div>


    <div class="flex-grow-1 overflow-auto">
        <ul class="list-group list-group-flush">
            @if (Model != null && Model.Any())
            {
                foreach (var user in Model)
                {
                    <li class="list-group-item bg-dark text-white border-0 d-flex align-items-center py-2">
                        <i class="bi bi-person-circle fs-4 me-2 text-white"></i>
                        <div class="overflow-hidden" style="width: 100%;">
                            <h6 class="mb-0 text-truncate" style="max-width: 180px;">@user.FullName</h6>
                            <small class="text-secondary d-flex justify-content-between" style="width: 100%;">
                                <span class="d-inline-block text-truncate" style="max-width: 140px;" title="@user.Email">
                                    @user.Email
                                </span>
                                <span class="text-info flex-shrink-0">@user.RoleName</span>
                            </small>
                        </div>
                    </li>
                }
            }
            else
            {
                <li class="list-group-item bg-dark text-white border-0 d-flex align-items-center py-2">
                    <div class="text-center w-100 text-secondary">
                        No users found.
                    </div>
                </li>
            }
        </ul>
    </div>
</div>*@
